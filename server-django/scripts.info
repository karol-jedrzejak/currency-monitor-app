# istalacja venv
python -m venv venv  

# aktywacja venv
venv/Scripts/activate

# isntalacja django
pip install django

# nowy projekt
django-admin startproject currency_server_app .

# nowy app
django-admin startapp nazwa_app

# utworzenie requirements.txt
pip freeze > requirements.txt

# library for postres
pip install "psycopg[binary]"

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": 'postgres',
        "USER": 'postgres',
        "PASSWORD": 'mypassword',
        "HOST": 'localhost',
        "PORT": 5432,
    }
}

docker-compose up
docker-compose ps
docker-compose start
docker-compose stop

python manage.py makemigrations
python manage.py migrate

#uruchamianie
python manage.py runserver
